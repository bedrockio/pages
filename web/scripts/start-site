#!/usr/bin/env node

require('@bedrockio/node-warnings');

// Args

const { program } = require('commander');

program
  .name('start-site')
  .description('Starts the dev server and runs the app.');

program.parse();

require('../serve/dev');
